<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myfg">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/fglogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFG">
        <activity android:name=".activities.DineInActivity"></activity>
        <activity android:name=".activities.AddTimingActivity" />
        <activity android:name=".activities.SplashCreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.PaymentActivity" />
        <activity android:name=".activities.AddAddressActivity" />
        <activity android:name=".activities.AddressActivity" />
        <activity android:name=".activities.PlacedOrderActivity" />
        <activity android:name=".activities.NavCategoryActivity" />
        <activity android:name=".activities.DetailedActivity" />
        <activity android:name=".activities.ViewAllActivity" />
        <activity android:name=".activities.welcomeActivity" />
        <activity android:name=".activities.RegistrationActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>